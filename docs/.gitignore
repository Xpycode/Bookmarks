# macOS
.DS_Store

# Archive (internal artifacts, not for distribution)
.archive/

# Local/personal files (not part of the system)
docs-browser.html
docs.sh

# Session logs ARE the artifact in a consuming project — tracked.
# (The original framework rule `sessions/*.md` was for the LLM-Directions
# template repo itself, which doesn't want consumer session-log pollution.)

# Syncthing temp files
.syncthing.*.tmp
*.sync-conflict-*

# Embedding cache
.fastembed_cache/

# Serena tool cache
.serena/

# Skills managed externally by npx skills
.claude/skills/
.agents/

# But keep Claude settings as reference
!.claude/settings.local.json

.cowork/
